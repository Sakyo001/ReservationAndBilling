<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hotel Reservation Login</title>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #E5D8CEFF;
        }

        /* Custom Styles */
        .navbar-container {
            width: 95%;
            margin: 20px auto;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
        }

        .navbar {
            background-color: #fff;
            padding: 2px 30px;
            border-radius: 20px;
        }

        .navbar-brand img {
            height: 90px;
            width: 90px;
        }

        .navbar-title {
            font-weight: bold;
            color: #333;
            font-size: 24px;
            margin-left: 10px;
        }

        .btn-login, .btn-signup {
            color: #fff;
            background-color: #8A5A44;
            border: none;
            padding: 12px 40px;
            margin-left: 10px;
            border-radius: 5px;
        }

        /* Content Section */
        .content-section {
            display: flex;
            justify-content: space-between;
            width: 95%;
            margin: 40px auto;
            padding: 20px;
            height: 500px;
        }

        .content-left, .content-right {
            width: 48%;
        }

        .content-left {
            display: flex;
            flex-direction: column;
            justify-content: center;
            padding-right: 20px;
        }

            .content-left h2 {
                font-size: 70px;
                color: #3B1F00FF;
                font-weight: bold;
                margin-bottom: 15px;
            }

            .content-left p {
                font-size: 16px;
                color: #3B1F00FF;
                margin-bottom: 20px;
            }

        .btn-explore {
            background-color: #3B1F00FF;
            color: #fff;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            font-weight: bold;
            text-align: center;
            width: fit-content;
        }

        .content-right img {
            width: 100%;
            height: 100%;
            border-radius: 8px;
            object-fit: contain;
        }
    </style>
</head>
<body>

    <!-- Navbar Container for Floating Effect -->
    <div class="navbar-container">
        <nav class="navbar navbar-expand-lg">
            <a class="navbar-brand" href="#">
                <img src="~/Content/Images/logo.png" alt="Logo"> <!-- Update with the actual logo path -->
            </a>
            <span class="navbar-title">INTEGRATED BOOKING & BILLING</span>

            <div class="ml-auto">
                <a href="SignIn" class="btn btn-login">Login</a>
                <a href="SignUp" class="btn btn-signup">Sign Up</a>
            </div>
        </nav>
    </div>

    <!-- Two-Column Content Section -->
    <div class="content-section">
        <!-- Left Section -->
        <div class="content-left">
            <h2>Elevate 
<br /> Every Stay!</h2>
            <p>Transform ordinary visits into unforgettable experiences by personalizing each interaction. Utilize guest profiles to curate tailored services, from welcome amenities to customized recommendations, ensuring every guest feels uniquely valued.</p>
            <button class="btn-explore">Explore</button>
        </div>

        <!-- Right Section -->
        <div class="content-right">
            <img src="~/Content/Images/image1.jpg" alt="Hotel Room"> <!-- Update with the actual image path -->
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>